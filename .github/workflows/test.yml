name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    name: Automated test action
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: JSON to DotEnv
        uses: the-pistol/github-action-json-to-dotenv@master
        id: abc
        with:
          input-json: '.github/workflows/test-1.json'

#      - name: Show results
#        run: |
#          echo "key1:"
#          echo "${{ steps.json-dotenv.outputs.key1 }}"
#          echo "key2:"
#          echo "${{ steps.json-dotenv.outputs.key2 }}"

      - name: Run2
        id: run2
        run: |
          echo "${{ steps.abc.outputs.VAR1 }}"
          echo "${{ steps.abc.outputs.VAR2 }}"